# X投稿文案：Claude Codeの「スキル」の意味について

## 案1（機能説明・具体的）

> Claude Codeの「スキル」は、繰り返し使う指示や手順をMarkdownファイルにまとめて再利用できる仕組み。
> CLAUDE.mdと違って必要な時だけ読み込まれるから、コンテキストを圧迫しない。
> 名前で呼び出しても、説明文からClaudeに自動検出させてもOK。
> 毎回同じ指示を書く手間、これで終わりにできる。
>
> #ClaudeCode #AI駆動開発

## 案2（課題提起型・共感を誘う）

> 「毎回同じプロンプトを書くの地味にしんどい」
> → Claude Codeの「スキル」はその解決策。
> 手順やルールをMarkdownにまとめておけば、必要な時だけ自動で読み込まれる。
> CLAUDE.mdは常時ロードだけど、スキルは呼ばれた時だけ。
> 地味だけど効く、AI駆動開発の生産性ハック。
>
> #ClaudeCode

## 案3（Q&A形式・教育的）

> Claude Codeの「スキル」とは何か？
> → よく使う指示・チェックリスト・手順をまとめた"再利用可能な知識パッケージ"。
> /コマンドで手動起動も、descriptionからClaudeが自動検出することも可能。
> 必要な時だけ読み込むので、常駐するCLAUDE.mdとは役割が違う。
>
> #ClaudeCode #AIエージェント

## 案4（短文・キャッチー）

> Claude Codeの「スキル」＝Claudeに"得意技"を教える仕組み。
> 手順書をMarkdownで書いておくだけで、必要な場面だけ自動で呼び出してくれる。
> 毎回説明し直す時代は終わり。
>
> #ClaudeCode

---

### 背景メモ（参考情報）
- スキルは `.claude/skills/SKILL.md` にYAMLフロントマター付きMarkdownとして定義される。
- CLAUDE.mdは常時ロードされるのに対し、スキルは呼び出された時だけ読み込まれるためコンテキストコストが低い。
- 呼び出し方法：`/skill-name` による手動起動、または `description` フィールドをもとにClaudeが自動検出。
- プラグイン（設定込みで配布）、サブエージェント（独立実行）、MCPサーバー（外部ツール連携）とは役割が異なる。
